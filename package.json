{
  "scripts": {
    "build": "npx playwright install",
    "postinstall": "./script.sh && playwright install"
  },
  "dependencies": {
    "playwright": "^1.42.0"
  },
  "devDependencies": {
    "playwright": "^1.44.1"
  }
}
